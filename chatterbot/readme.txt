To run the chatbot .You have to install python 3.7.4 or later.
Then go to cmd and write pip install Flask
then pip install chatterbot.
Then open the cmd in the director where Flask_chatbot.py is created.
Then type the command python Flask_chatbot.py
Then you will get a link of server.
Then you copy the link and paste it to the browser.
Enjoy ......
